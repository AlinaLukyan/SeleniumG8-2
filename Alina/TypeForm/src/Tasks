
ToDo:
- WebDriverFactory (Singleton)
- try-catch if needed
- WebElements & ConfigData classes
- Fix annotations
- Fix all the tests
- Asserts for every test
- Spring config (Default wait constants)
- Actions, Select, Alert, WebDriverWait
- Varargs
-